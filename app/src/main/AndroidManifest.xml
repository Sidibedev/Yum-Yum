<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mouhamedalysidibe.restaurant">

    <application
        android:allowBackup="true"
        android:logo="@drawable/ic_action_icone"
        android:icon="@drawable/ic_action_icone"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menus"/>
        <activity android:name=".Plats"/>
        <activity android:name=".boissonActivity"/>
        <activity android:name=".Note"/>
        <activity android:name=".DetailPlat"/>
        <activity android:name=".Panier"/>
    </application>

</manifest>